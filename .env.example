# Database - Neon PostgreSQL (via Vercel integration)
# For local development, copy DATABASE_URL from Vercel project environment variables
# DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# For production, DATABASE_URL is automatically configured via Vercel Neon integration